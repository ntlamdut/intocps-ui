export class FmuImploder
{
    public static implodeConfig(path: string) {
        let coSimConfig: CoSimulationConfig;
        CoSimulationConfig.parse(this.path, project.getRootFilePath(), project.getFmusPath())
        


    }
}